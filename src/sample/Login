.root {-fx-background-image: url ("blurry-lights-background")}
.label {
    -fx-font-size: 12px;
    -fx-font-weight: bold;
    -fx-font-fill: #0E1D33;
}

#login-text {
    -fx-font-size:32px;
    -fx-font-family: "Arial Black";
    -fx-fill: #115E7F;
}

#actiontarget {
    -fx-fill:WHITE;
    -fx-font-weight:bold;
}

.button {
    -fx-text-fill:white;
    -fx-font-family:"Arial Narrow";
    -fx-font-weight:bold;
    -fx-background-color:linear-gradient(#61a2b1, #2A5058);
}